import { IsraelidatePipe } from './israelidate.pipe';

describe('IsraelidatePipe', () => {
  it('create an instance', () => {
    const pipe = new IsraelidatePipe();
    expect(pipe).toBeTruthy();
  });
});
